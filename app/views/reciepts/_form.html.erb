<%= form_with(model: reciept, local: true) do |form| %>
  <% if reciept.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reciept.errors.count, "error") %> prohibited this reciept from being saved:</h2>

      <ul>
      <% reciept.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
